# opsrunbook-copilot

Iteration 3: Orchestrator + Evidence Collectors + Stub Analyzer → IncidentPacket

## Quickstart (local)
1) Create a virtualenv and install:
```bash
python -m venv .venv
source .venv/bin/activate
pip install -e ".[dev]"
